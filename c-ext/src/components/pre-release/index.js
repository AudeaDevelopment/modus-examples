import React, { Component } from 'react';
import { withRouter } from 'react-router-dom';

class PreRelease extends Component {
  render() {
    return <img style={{ height: '100%', width: '100%' }} src="" />;
  }
}

export default withRouter(PreRelease);
